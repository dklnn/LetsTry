<h1>Current post:</h1>
<div>
  <%= @post.id %>
</div>
<div>
  <%= @post.title %>
</div>
<div>
  <%= @post.body %>
</div>
<div>
  <%= User.find(@post.user_id).name %>
</div>
<div>
  <%= @post.created_at.strftime("%k:%M") %>
</div>
<div>
  <%= @post.created_at.strftime("%d|%m|%Y") %>
</div>
<br/>
<h2>Comments</h2>
<u1>
  <% @post.comments.each do |comment| %>
  <% params = { :post_id => comment.post_id, :id => comment.id } %>
    <li>
      <table>
        <td>
          <%= comment.body %>
        </td>
        <td>
          <% user = User.find(comment.user_id) %>
          <%= user.name %>
        </td>
        <td>
          <%= button_to 'Edit', edit_post_comment_path(params), method: :put %>
        </td>
        <td>
          <% params = { :post_id => comment.post_id, :id => comment.id } %>
          <%= button_to 'Destroy', destroy_post_comment_path(params), method: :delete %>
        </td>
      </table>
    </li>
  <% end %>
</u1>
<%= form_with model: @post.comments.build, url: [@post, @post.comments.build] do |f| %>
  <div>
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>
<%= link_to 'Back', posts_path %>
