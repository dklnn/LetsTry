<h1>Editing comment</h1>
<%= form_with model: comment do |f| %>
  <div>
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>
<%= link_to "Back", post_path(post) %>