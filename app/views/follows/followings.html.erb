<%= link_to 'Back', posts_path %>
<h4>Followings count: <%= @follows.count %></h4>
<% @follows.each do |follow| %>
  <div>
    <% user = User.find(follow.followee_id) %>
    <%= link_to user.name, user_path(user), method: :get %>
  </div>
<% end %>