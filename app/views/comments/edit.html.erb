<h1>Editing comment</h1>
<%= form_with model: @comment, url: post_comment_path(params[:post_id]) do |f| %>
    <%= f.text_area :body %>
    <%= f.submit %>
<% end %>
<%= link_to "Back", post_path(params[:post_id]) %>
